import React from 'react';
import { logoutUser } from '../api/auth-apis';
import Login from '../components/auth/Login';

const LoginPage = () => {
    return <Login />;
};

export default LoginPage;
